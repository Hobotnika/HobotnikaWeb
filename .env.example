# Database Configuration
# PostgreSQL connection string (required for Drizzle ORM)
# Format: postgresql://user:password@host:port/database
# For local development, you can use a local PostgreSQL instance or a cloud provider like Neon
DATABASE_URL=postgresql://user:password@localhost:5432/hobotnikaweb

# Server Configuration
# Port for the Express server (optional, defaults to 5000)
PORT=5000

# Node Environment
# Set to 'development' for local dev, 'production' for production builds
NODE_ENV=development
